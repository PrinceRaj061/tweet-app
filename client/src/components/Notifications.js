import React from 'react';

function Notifications({ token }) {
    return (
        <div className="container">
            <h1>Notifications</h1>
            {/* Notifications details can be fetched and displayed here */}
        </div>
    );
}

export default Notifications;
