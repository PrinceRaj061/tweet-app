import React from 'react';

function Account({ token }) {
    return (
        <div className="container">
            <h1>Account Information</h1>
            {/* Account details can be fetched and displayed here */}
        </div>
    );
}

export default Account;
